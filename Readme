This project enable to connect to Azure IotHub, send data to IotHub, 
receive data from build-in endpoint EventHub, and seve data into postgresql database  
Every credential are stored in external file env.py, not attach to this repository.
